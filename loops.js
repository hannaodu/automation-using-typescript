//---loops----
let region = ["us-east-1", "us-west-2", "eu-central-1"];
for (let i = 0; i < region.length; i++) {
  console.log(region[i]);
}

//---array of cloud service ---
let cloudService = ["ec2", "s3", "lambda"];
for (let i = 0; i < cloudService.length; i++) {
  console.log(cloudService[i]);
}
